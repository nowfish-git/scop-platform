/**
 * SCOP 共享类型定义
 *
 * 当前为 M0 骨架占位，类型定义将在 M1.1 阶段填充:
 * - common/response.interface.ts — ApiResponse<T>, PaginatedResponse<T>
 * - common/pagination.interface.ts — PaginationQuery, PaginationMeta
 * - user/ — User entity, DTO, enum
 * - auth/ — Login/Register DTO, JwtPayload
 * - alert/ — Alert entity, DTO, enum
 * - community/, device/, elder/ — 各中心领域类型
 */

export {};
